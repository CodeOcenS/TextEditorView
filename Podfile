use_frameworks!

platform :ios, '9.0'

target 'TextEditorView_Example' do
  pod 'TextEditorView', :path => '../'

  target 'TextEditorView_Tests' do
    inherit! :search_paths

    #pod 'Quick', '~> 2.2.0'
    #pod 'Nimble', '~> 8.0.7'
  end
end
